#:import dp kivy.metrics.dp
<StatsScreen>:
    MDBoxLayout:
        orientation: 'vertical'
        spacing: dp(10)
        padding: dp(20)

        MDTopAppBar:
            id: app_bar
            title: '[統計表示画面]'

        MDRaisedButton:
            text: '戻る'
            on_release: root.change_screen('menu')
